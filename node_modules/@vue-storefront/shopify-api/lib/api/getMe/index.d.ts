import { CustomQueryFn } from '../../index';
export interface GetMeParams {
    customer?: boolean;
}
export interface OrdersData {
    me: any;
}
declare const getMe: ({ config }: {
    config: any;
}, params?: GetMeParams, customQueryFn?: CustomQueryFn) => {};
export default getMe;
