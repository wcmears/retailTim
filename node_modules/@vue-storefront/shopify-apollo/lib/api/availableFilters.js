import { gql } from '@apollo/client/core';
const DEFAULT_QUERY = gql `
query filters($handle: String!) {
    collectionByHandle(handle: $handle) {
      handle
      products {
        filters {
          id
          label
          type
          values {
            id
            label
            count
            input
          }
        }
      }
    }
}
`;
export default async function availableFilters(context, params, customQuery) {
    const variables = {
        handle: params.handle
    };
    const { filters } = context.extendQuery(customQuery, {
        filters: {
            query: DEFAULT_QUERY,
            variables
        }
    });
    const response = await context.client.apolloClient.query({
        query: filters.query,
        variables: filters.variables
    });
    return response ?? null;
}
//# sourceMappingURL=availableFilters.js.map