import { gql } from '@apollo/client/core';
const DEFAULT_MUTATION = gql `
    mutation customerCreate($input: CustomerCreateInput!) {
        customerCreate(input: $input) {
            customer {
              id
            }
            customerUserErrors {
              code
              field
              message
            }
          }
    }
`;
export default async function customerCreate(context, params) {
    const variables = {
        input: params
    };
    const response = await context.client.apolloClient.mutate({
        mutation: DEFAULT_MUTATION,
        variables
    });
    return response.data.customerCreate;
}
//# sourceMappingURL=customerCreate.js.map