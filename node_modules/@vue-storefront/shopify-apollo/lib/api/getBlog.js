import { GetBlogQuery } from '../query/GetBlog.gql';
export async function getBlog(context, params, customQuery) {
    const { blog } = context.extendQuery(customQuery, {
        blog: {
            query: GetBlogQuery,
            variables: params
        }
    });
    const response = await context.client.apolloClient.query({
        query: blog.query,
        variables: blog.variables
    });
    return response ?? null;
}
//# sourceMappingURL=getBlog.js.map