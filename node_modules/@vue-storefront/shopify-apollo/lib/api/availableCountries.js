import { gql } from '@apollo/client/core';
export async function availableCountries(context, customQuery) {
    const localizationQuery = gql `
  query getLocalization {
    localization {
      availableCountries {
        name
        isoCode
        currency {
          isoCode
          name
          symbol
        }
      }
    }
  }
`;
    const { availableCountries } = context.extendQuery(customQuery, {
        availableCountries: {
            query: localizationQuery,
            variables: {}
        }
    });
    const response = await context.client.apolloClient.query({
        query: availableCountries.query
    });
    return response?.data?.localization?.availableCountries ?? [];
}
//# sourceMappingURL=availableCountries.js.map