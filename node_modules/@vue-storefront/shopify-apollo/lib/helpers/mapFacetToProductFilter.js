export function mapFacetToProductFilter(filters) {
    function getPriceFilters() {
        if (!filters.min && !filters.max)
            return {};
        return {
            price: {
                ...(filters.min && { min: parseFloat(filters.min) }),
                ...(filters.max && { max: parseFloat(filters.max) })
            }
        };
    }
    return {
        ...getPriceFilters()
    };
}
//# sourceMappingURL=mapFacetToProductFilter.js.map